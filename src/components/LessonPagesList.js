import React, {PropTypes, Component} from 'react';

class LessonPagesList extends Component {

    static propTypes = {
        lesson: PropTypes.object //array of pages, name
    }

    render() {
        return (
            <div />
        );
    }
}

export default LessonPagesList;
